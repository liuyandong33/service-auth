<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.auth.mappers.PrivilegeMapper">
    <select id="obtainUserPosPrivileges" resultType="build.dream.common.domains.saas.PosPrivilege">
        SELECT
        pos_privilege.*
        FROM pos_privilege
        INNER JOIN pos_role_privilege_r ON pos_privilege.id = pos_role_privilege_r.privilege_id
        INNER JOIN pos_role ON pos_role.id = pos_role_privilege_r.pos_role_id AND pos_role.deleted = 0
        INNER JOIN user_pos_role_r ON pos_role.id = user_pos_role_r.pos_role_id AND user_pos_role_r.user_id = #{userId}
        WHERE pos_privilege.deleted = 0
    </select>
</mapper>